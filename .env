# Expo only inlines vars prefixed with EXPO_PUBLIC_ into the app bundle.
# (Plain vars like API_URL are silently ignored at runtime.)

# Authentication Mode — 'mock' for demo/local testing, 'api' for backend integration
EXPO_PUBLIC_AUTH_MODE=api

# Backend API base URL (must include the /api/v1 prefix).
#   Physical device : http://<your-LAN-IP>:3000/api/v1   (this machine: 192.168.1.7)
#   Android emulator: http://10.0.2.2:3000/api/v1
#   iOS simulator   : http://localhost:3000/api/v1
EXPO_PUBLIC_API_URL=http://192.168.1.7:3000/api/v1
EXPO_PUBLIC_API_TIMEOUT=10000
